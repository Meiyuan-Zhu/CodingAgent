# Demo Workspace

This is a small, safe workspace used while building the local coding agent.

The backend is configured to treat this directory as the only file area that agent tools may inspect during development.
